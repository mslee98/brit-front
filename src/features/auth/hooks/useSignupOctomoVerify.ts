/**
 * useSignupOctomoVerify — OCTOMO 가입 플로우 제거됨 (관리자 승인 모델).
 * 파일은 복구·레거시 참조용으로 남겨 두며, 호출하면 에러.
 */
export function useSignupOctomoVerify(): never {
  throw new Error('OCTOMO 가입 인증은 제거됐어요. 관리자 승인 플로우를 사용해 주세요.')
}
