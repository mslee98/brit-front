import { useEffect } from 'react'

/**
 * Push 클릭·향후 소켓 Inbox 부트스트랩 자리.
 * mock OS 알림 소스는 P0에서 제거했다.
 */
export function NotificationBootstrap() {
  useEffect(() => {
    // reserved for SSE/WS NotificationSource.connect
  }, [])

  return null
}
